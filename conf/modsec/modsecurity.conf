# =============================================================================
# ModSecurity v3 — Hardened Configuration
# Mode: BLOCKING (On) — not DetectionOnly
# =============================================================================

# -- Rule engine initialization ----------------------------------------------
# BLOCKING MODE: actively blocks malicious requests
SecRuleEngine On

# -- Request body handling ---------------------------------------------------
SecRequestBodyAccess On

# XML parser
SecRule REQUEST_HEADERS:Content-Type "(?:application(?:/soap\+|/)|text/)xml" \
     "id:'200000',phase:1,t:none,t:lowercase,pass,nolog,ctl:requestBodyProcessor=XML"

# JSON parser
SecRule REQUEST_HEADERS:Content-Type "application/json" \
     "id:'200001',phase:1,t:none,t:lowercase,pass,nolog,ctl:requestBodyProcessor=JSON"

# Handle +json subtypes (APIs)
SecRule REQUEST_HEADERS:Content-Type "^application/.+[+]json$" \
     "id:'200006',phase:1,t:none,t:lowercase,pass,nolog,ctl:requestBodyProcessor=JSON"

# Request body limits (hardened — reduced from defaults)
SecRequestBodyLimit 10485760
SecRequestBodyNoFilesLimit 65536
SecRequestBodyLimitAction Reject

# JSON depth limit (prevent deep nesting attacks)
SecRequestBodyJsonDepthLimit 64

# Arguments limit
SecArgumentsLimit 500

SecRule &ARGS "@ge 500" \
    "id:'200007',phase:2,t:none,log,deny,status:400,msg:'Too many arguments',severity:2"

# Verify request body parsing
SecRule REQBODY_ERROR "!@eq 0" \
    "id:'200002',phase:2,t:none,log,deny,status:400,msg:'Failed to parse request body.',logdata:'%{reqbody_error_msg}',severity:2"

# Strict multipart validation
SecRule MULTIPART_STRICT_ERROR "!@eq 0" \
    "id:'200003',phase:2,t:none,log,deny,status:400, \
    msg:'Multipart request body failed strict validation: \
    PE %{REQBODY_PROCESSOR_ERROR}, \
    BQ %{MULTIPART_BOUNDARY_QUOTED}, \
    BW %{MULTIPART_BOUNDARY_WHITESPACE}, \
    DB %{MULTIPART_DATA_BEFORE}, \
    DA %{MULTIPART_DATA_AFTER}, \
    HF %{MULTIPART_HEADER_FOLDING}, \
    LF %{MULTIPART_LF_LINE}, \
    SM %{MULTIPART_MISSING_SEMICOLON}, \
    IQ %{MULTIPART_INVALID_QUOTING}, \
    IP %{MULTIPART_INVALID_PART}, \
    IH %{MULTIPART_INVALID_HEADER_FOLDING}, \
    FL %{MULTIPART_FILE_LIMIT_EXCEEDED}'"

SecRule MULTIPART_UNMATCHED_BOUNDARY "@eq 1" \
    "id:'200004',phase:2,t:none,log,deny,msg:'Multipart parser detected a possible unmatched boundary.'"

# -- PCRE Tuning (prevent ReDoS) --------------------------------------------
SecPcreMatchLimit 1000
SecPcreMatchLimitRecursion 1000

SecRule TX:/^MSC_/ "!@streq 0" \
    "id:'200005',phase:2,t:none,deny,msg:'ModSecurity internal error flagged: %{MATCHED_VAR_NAME}'"

# -- Response body handling --------------------------------------------------
SecResponseBodyAccess On
SecResponseBodyMimeType text/plain text/html text/xml application/json
SecResponseBodyLimit 524288
SecResponseBodyLimitAction ProcessPartial

# -- Filesystem configuration (hardened — dedicated private dirs) ------------
SecTmpDir /var/lib/modsecurity/tmp/
SecDataDir /var/lib/modsecurity/data/

# -- Audit log configuration ------------------------------------------------
SecAuditEngine RelevantOnly
SecAuditLogRelevantStatus "^(?:5|4(?!04))"
SecAuditLogParts ABCFHZ
SecAuditLogType Serial
SecAuditLog /var/log/nginx/modsec_audit.log

# -- Miscellaneous -----------------------------------------------------------
SecArgumentSeparator &
SecCookieFormat 0
SecUnicodeMapFile unicode.mapping 20127

# DISABLED: do not leak version info to upstream telemetry
SecStatusEngine Off

# -- Additional hardening rules ----------------------------------------------

# Block requests with both Content-Length and Transfer-Encoding (smuggling)
SecRule REQUEST_HEADERS:Content-Length "." \
    "id:'200010',phase:1,t:none,chain,deny,status:400,msg:'HTTP Request Smuggling detected'"
    SecRule REQUEST_HEADERS:Transfer-Encoding "."

# Block NULL bytes in request
SecRule ARGS|ARGS_NAMES|REQUEST_HEADERS "@contains \x00" \
    "id:'200011',phase:2,t:none,deny,status:400,msg:'NULL byte in request'"
